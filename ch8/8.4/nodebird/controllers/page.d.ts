import { type RequestHandler } from 'express';
declare const renderProfile: RequestHandler;
declare const renderJoin: RequestHandler;
declare const renderMain: RequestHandler;
declare const renderHashtag: RequestHandler;
export { renderHashtag, renderProfile, renderMain, renderJoin };
